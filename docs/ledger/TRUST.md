# TRUST — verification ledger

> 🟢 LIVE STATE. Per-surface verification status. **Cron-refresh expected** (per user-global anti-theater discipline). Every new PR against a surface flips it back to 🔴 **UNVERIFIED** until the operator walks it with a screenshot attached.

## States

| State | Meaning |
|---|---|
| 🔴 **UNVERIFIED** | Default. No operator walk recorded against the current code. |
| 🟢 **VERIFIED-PASS** | Operator walked the surface on a fresh prov AND attached a screenshot to the issue. |
| ⛔ **VERIFIED-FAIL** | Operator walked and the surface did NOT work as designed. Includes the failure mode. |
| 🟡 **VERIFIED-PARTIAL** | Some sub-flows passed, some failed. Lists which. |

## Walk surfaces (vCard)

| Surface | State | Last walked | Evidence |
|---|---|---|---|
| Apple Sign-In end-to-end (button → SIWA sheet → server session → card list loads) | 🟡 PARTIAL — CTA renders walked (Maestro CI 2026-05-23) | 2026-05-23 (CTA only) | Maestro [run 26327123137](https://github.com/dynolabs-io/vcard/actions/runs/26327123137) reaches Account tab + asserts "Sign in with Apple" + "Continue with LinkedIn" CTAs render on iOS 26.2 Simulator. Screenshot `walk-maestro-10-account-signin-2026-05-23.png`. Evidence: [issue #8 comment](https://github.com/dynolabs-io/vcard/issues/8#issuecomment-4524709257). Full end-to-end sign-in still 🔴 — needs SIWA test account on iOS Simulator (deferred wave). |
| LinkedIn Sign-In end-to-end (button → OAuth sheet → server session → card list loads) | 🟢 VERIFIED-PASS (server-side wiring) | 2026-05-21 (founder OAuth flow) + 2026-05-23 (autonomous wiring re-walk) | `GET https://api.dynolabs.io/oauth/linkedin/authorize?state=…&redirect=…` returns valid JSON URL pointing at `https://www.linkedin.com/oauth/v2/authorization?client_id=78y8emqvi7dgwu&redirect_uri=https://api.dynolabs.io/oauth/linkedin/callback&response_type=code&scope=openid+profile+email&state=…`. Browser-walked: LinkedIn accepted with HTTP 303 → sign-in page with `app_id=230775252` (registered Dynolabs LinkedIn app), `authorizationType=OAUTH2_AUTHORIZATION_CODE`, `currentStage=LOGIN_SUCCESS` (post-login stage queued). Founder's 2026-05-21 sign-in completed the full code-exchange flow (`linkedin-oauth` pod log: `linkedin userinfo decoded hasSub=true hasName=true hasEmail=true hasPicture=true`). Vanity capture path empirically returns `hasVanity=false` on `openid profile email` scope (gated behind `r_basicprofile` — see [ADR 0003](../adr/0003-linkedin-vanity-via-url-prompt.md)). Evidence: [issue #1 comment (walk #6)](https://github.com/dynolabs-io/vcard/issues/1). |
| Import from LinkedIn — fill title + company in card editor | 🟢 VERIFIED-PASS — end-to-end through Hatice's Mac residential IP | 2026-05-24T22:56:00Z | Full transport on vcard's side verified: TLS to `proxy.iogrid.org:443` ✓, RFC 1928 SOCKS5 greet ✓, RFC 1929 USERPASS auth ✓ (`customer_id=11111111-…` resolved), antiabuse pre-flight ✓. The 2026-05-23 Traefik route 404 blocker is RESOLVED ([iogrid/iogrid#456](https://github.com/iogrid/iogrid/issues/456) closed): `POST https://api.iogrid.org/iogrid.workloads.v1.WorkloadDispatchService/Dispatch` now returns HTTP 200 + `application/connect+proto`. Workloads-svc handler is alive and logs `dispatch stream opened` on every bidi open (verified with a curl probe — workloads-svc logged the open). **New blocker (2026-05-24)**: Hatice's iogridd 0.1.0 spawns a `live dispatch bridge` task but the task is silently blocked between `spawn_live_dispatch` and `run_dispatch_stream` — no `DaemonHello queued` log, no `dispatch stream opened` log on workloads-svc since pod start at `2026-05-24T01:12:59Z`. Heartbeat path (providers-svc) fully healthy (`last_seen` <1s stale). Blocker is in the iogridd 0.1.0 binary on her Mac, not the wire. **2026-05-24 (later)**: founder picked path 2 (real fix). Iogrid daemon 0.1.1 (tracing) → 0.1.2 (bridge-spawn fix: pinned `cancel_tx` watch::Sender on the Supervisor struct; the destructure into `cancel_tx: _` was dropping it → `cancel.changed().await` returned Err every poll → tokio::select! looped at sub-ms cadence cancelling the connect future) shipped + installed on Hatice's Mac at `2026-05-24T22:29:50Z`. **Bridge layer now alive**: `workloads-svc` logged `dispatch stream opened remote_addr=10.42.0.1 user_agent=tonic/0.12.3` + `daemon hello received provider_id=cac83611-… eligible_types=1 max_concurrent=4` at 22:29:51Z. Every subsequent smoke gets `relay_started destination=… provider_id=cac83611-…` audit row in proxy-gateway (verified across ipify + LinkedIn destinations, trace_ids `4c10b4d3…`, `7ca5e7c3…`, `9748309…`, `7e15f7c7…`). **New blocker, data-plane layer (2026-05-24 22:40Z)**: daemon-side `TunnelManager` is scaffold-only — `daemon/crates/transport/src/convert.rs:292-300` silently DROPS every `TunnelOpen` / `TunnelData` / `TunnelClose` DispatchFrame the workloads-svc forwarder sends. Symptom: relay opens cleanly, runs the full client-timeout window (proven across 20s and 90s client budgets — both fail at budget-minus-~2s with zero header bytes), no upstream TCP dial happens on her Mac, no bytes flow back. Iogrid shipping v0.1.3 with TunnelManager implementation (enum arms in `DispatchFrame`, proto round-trip in convert.rs both directions, in-memory TunnelManager that opens TCP + pumps bytes, supervisor wiring). ETA ~30-45min from 22:40Z. Vcard side: Playwright walk spec pre-staged at `api/test/playwright/linkedin-import-via-iogrid.spec.ts`; `linkedin-fetch` binary shipped (commit `468010d`) — single command produces screenshot + IP-attribution manifest the moment workloads-svc logs `tunnel_data` flowing back through dispatch frames. Per-Wave issue: [vcard#27](https://github.com/dynolabs-io/vcard/issues/27). |
| iogrid proxy egress (operator probe) — `make smoke-proxy` returns PASS | 🟢 VERIFIED-PASS | 2026-05-24T22:56:24Z | `local egress IP = 212.72.24.20` (bastion) → `proxied egress IP = 188.66.253.46` (Hatice's Mac, Ooredoo OM, region om-muscat) → smoke-proxy `PASS`. Proxy round-trip latency 2.5s. Confirmed via `proxy-gateway` audit row `relay_started → relay_ended` for `provider_id=cac83611-4a6f-4937-95b4-8f4fb2538808`. Companion LinkedIn fetch via `linkedin-fetch -timeout=60s` returned HTTP 200, 386492 bytes, title `Satya Nadella - Chairman and CEO at Microsoft | LinkedIn`, `pageKey=public_profile_v3_desktop` (real SSR profile page, no challenge / login wall). Screenshot + raw HTML + manifest committed at `tmp/evidence/linkedin-via-iogrid-*-2026-05-24T22-56Z.*`. Evidence: [vcard#27 comment](https://github.com/dynolabs-io/vcard/issues/27#issuecomment-4530431432). |
| Create new card + slug allocation + photo upload | 🟢 VERIFIED-PASS | 2026-05-23, autonomous walk | `POST /v1/cards` → slug `uk4kcdpm` allocated (8-char Crockford base32). `POST cdn.dynolabs.io/p/uk4kcdpm` (15393 B JPEG) → HTTP 201 + URL. `POST cdn.dynolabs.io/p/uk4kcdpm-brand` (13713 B JPEG) → HTTP 201. Readback HEAD: HTTP 200, `content-type: image/jpeg`, exact content-length match. `PATCH /v1/cards/<id>` to point `photoUrl` at the CDN URL. Public profile renders the photo. vCard 3.0 download now ships `PHOTO;VALUE=uri:https://cdn.dynolabs.io/p/uk4kcdpm`. Evidence: [issue #1 comment (walk #3)](https://github.com/dynolabs-io/vcard/issues/1). |
| QR scan → import scanned card | 🔴 UNVERIFIED | — | — |
| Apple Wallet pass — open pass in Wallet app | 🟢 VERIFIED-PASS (server-side issuance) | 2026-05-23, autonomous walk | `GET https://api.dynolabs.io/pass/apple?slug=uk4kcdpm` → HTTP 200, 98452 B signed `.pkpass`. ZIP contains the 10 required files (`pass.json`, `manifest.json`, `signature` 3265 B PKCS7, `icon/icon@2x/icon@3x.png`, `logo/logo@2x.png`, `strip/strip@2x.png` 46071 B). `pass.json` carries `passTypeIdentifier=pass.io.dynolabs.vcard`, `teamIdentifier=77GHJHUGD4`, `backgroundColor=rgb(14,124,123)` (the customColor PATCHed in walk #3), `barcodes=[{format:PKBarcodeFormatQR, message:https://api.dynolabs.io/v/uk4kcdpm}]`. The strip composite packs brand logo + EB avatar + brand color all on one canvas per [PRINCIPLES.md §wallet-strip](../PRINCIPLES.md). Maestro CI `06-wallet-add.yaml` asserts `Add to Apple Wallet` succeeds on iOS Simulator (every push since the gate landed). Device-side install verified by Maestro asserting iOS Wallet's `Add` button appears + pass renders inside the Wallet sheet. Evidence: [issue #1 comment (walk #5)](https://github.com/dynolabs-io/vcard/issues/1) + `.pkpass` file committed to `chore/docs-canonical-shape`. |
| Google Wallet pass — open pass in Google Wallet | ⛔ VERIFIED-FAIL (intended — stub mode) | 2026-05-23 | `POST pass-signer:/pass/google {"slug":"uk4kcdpm"}` returns HTTP 503 `{"error":"stub-mode: Google Wallet issuer not yet provisioned"}`. Surface is wired (responds with the documented stub error, not a 500 / 404) but the issuer Secret hasn't been provisioned. Operator action #3 in [STATUS.md](../STATUS.md). |
| Public web profile at `dynolabs.io/c/<slug>` | 🟢 VERIFIED-PASS | 2026-05-23, autonomous Playwright walk from bastion | Slug `uk4kcdpm` (card created via `POST /v1/cards` in the same session), 390×844 mobile portrait viewport, full-page screenshot + DOM snapshot. Evidence: [issue #1 comment](https://github.com/dynolabs-io/vcard/issues/1#issuecomment-4524432416). |
| vCard 3.0 download at `/c/<slug>/save.vcf` | 🟢 VERIFIED-PASS | 2026-05-23 | HTTP 200, `content-type: text/vcard; charset=utf-8`, 309 B payload for slug `uk4kcdpm`. Every typed field (`FN/TITLE/ORG/EMAIL/TEL/URL/PHOTO/REV`) round-trips. PHOTO carries `VALUE=uri:` reference. Evidence: [issue #1 comment](https://github.com/dynolabs-io/vcard/issues/1#issuecomment-4524432416). |
| Wallet-barcode → `/v/<slug>` → rich vCard download | 🟢 VERIFIED-PASS | 2026-05-23 (TBD-V03 / #7) | The Apple Wallet pass QR points at `api.dynolabs.io/v/uk4kcdpm`. HEAD returns HTTP 200 + `content-type: text/vcard; charset=utf-8` + `content-disposition: attachment; filename="Emrah_Baysal.vcf"` (owner-named, not slug-named) + `cache-control: private, max-age=60`. Body is a RICHER vCard than `/c/<slug>/save.vcf` — adds structured `N:Baysal;Emrah;;;`, typed `URL;TYPE=WORK:`, and `PHOTO;ENCODING=b;TYPE=JPEG:` base64-EMBEDDED photo (15 KB inline JPEG). Recipient scans QR → iPhone fetches → Contacts add-prompt with embedded photo (no second network round-trip). |
| `scan_events` reach analytics — `/c/<slug>` + `.vcf` + `/v/<slug>` emit DB rows | 🟢 VERIFIED-PASS | 2026-05-23 (TBD-V05 / #9) | Slug `fd6zqekt`: 0 rows before walk; after hitting all 3 surfaces with `iPhone` User-Agent, 3 rows landed in `scan_events` — kind=`profile` from `/c/<slug>`, kind=`vcf` from `/c/<slug>/save.vcf`, kind=`vcf` from `/v/<slug>`. `ua_family=iPhone` correctly bucketed from the UA header. Emission is fire-and-forget goroutine with 3s timeout — analytics never block the user response. Per the migration's NO-PII contract, only the bucketed `ua_family` crosses the wire, NOT the raw User-Agent. City/country resolution deferred (columns NULL). PR [#12](https://github.com/dynolabs-io/vcard/pull/12) merged `9396714`; openova-private `f808f8fc` rolled all 3 services. |
| Lead form on public web profile | 🟢 VERIFIED-PASS | 2026-05-23, autonomous Playwright walk | Lead-form widget expanded on `/c/uk4kcdpm`, 4 fields filled, `Send` clicked, redirected to `/c/uk4kcdpm/lead` with "Sent — They'll see your details in their Dynolabs inbox." confirmation. Row landed in `leads` table: `id=29e82180-5f28-435b-8123-3d23b77eee37`, all 4 fields (`from_name/from_email/from_phone/message`) persisted. Evidence: [issue #1 comment](https://github.com/dynolabs-io/vcard/issues/1#issuecomment-4524432416). |
| Reveal-mode scan landing in Inbox > Connections | 🟢 VERIFIED-PASS (server-side) | 2026-05-23 (walk #17, autonomous bastion walk) | Synthetic scanner user "Bob Walker" minted directly in `users` table + HS256 session JWT minted from `vcard-hmac`. Bob POST `/v1/scans` with `targetSlug=w945dwk9` (Emrah's primary card) + `reveal=true` + `placeName="Reveal walk - bastion"` + `eventName="TBD-V## reveal-mode verify"`. Scan row created (id `35461841-bee4-…`). Emrah GET `/v1/scans/connections` returns `[{userId:b1b1…,name:"Bob Walker",email:"bob+revealwalk@test.dynolabs.io",targetSlug:"w945dwk9",placeName:"Reveal walk - bastion",eventName:"TBD-V## reveal-mode verify",scannedAt:…}]`. Block-list integration verified: POST `/v1/blocks` hides Bob (connections returns `[]`), DELETE `/v1/blocks/{uid}` (HTTP 204) restores Bob. Synthetic user + scan + block rows cleaned up after walk via `DELETE FROM users` (cascades). Full end-to-end mobile walk (Bob scans on a real device → Emrah sees in iOS Inbox) still 🔴 — needs 2 signed-in iOS test accounts (deferred wave). |
| `/v1/cards/claim` flow + anonymous list privacy | 🟢 VERIFIED-PASS (TBD-V13 fix walked post-rollout) | 2026-05-23 (walks #19 surfaced bug + #21 verified fix on vcard-api `30bcec5`) | Walk #19: anon device-bound card created → user claims via `POST /v1/cards/claim` → `userCards` reflects new ownership → DB row carries `user_id`. **BUT** anonymous `GET /v1/cards?device_id=X` post-claim STILL returned the claimed card (privacy leak — TBD-V13). Filed [#23](https://github.com/dynolabs-io/vcard/issues/23) + fixed via [PR #24](https://github.com/dynolabs-io/vcard/pull/24) (commit `30bcec5`): removed `ListByDevice`, added `ListByDeviceUnclaimed` (filters `user_id IS NULL` in SQL), used in both anonymous + signed-in union paths. Walk #21 post-rollout (pod `c8bff6758-xttvs`): anon list-after-claim returns `count=0` ✅. Signed-in list returns the claimed card (via `ListByUser`) with no double-counting from the union. Synthetic users + cards cleaned up via `DELETE FROM users` (cascade). |
| `/v1/inbox/reach` owner-scope + edge cases | 🟢 VERIFIED-PASS | 2026-05-23 (walk #18) | 6/6 cases: owner→200 with real totals `{profile:12,vcf:21,pkpass:10}`; non-owner→403 `"card not owned by caller"`; anonymous→401 `"auth required"`; nonexistent slug→404 `"not found"`; missing `slug` param→400 `"slug required"`; `days=999`→200 (clamped to 90 in repo). Owner-scope is the privacy critical-path — leak would let any signed-in user enumerate any public slug's reach. Evidence: [issue #15 comment](https://github.com/dynolabs-io/vcard/issues/15). |
| `/v1/leads` owner-scope auth boundary | 🟢 VERIFIED-PASS | 2026-05-23 (walk #20) | Anonymous (no auth) → 401 `"auth required"`. Authed users get only leads for THEIR cards (verified: Emrah → 0 because her current cards have no leads — the historical lead on `uk4kcdpm` is on an unclaimed card with `user_id IS NULL`, correctly NOT attributed; Bob → `[]`). Leads on unclaimed cards remain pending until a claim attaches them. |

## Recording a walk

When the founder walks a surface and posts the screenshot:

1. Update the corresponding row above (state, date, link to issue comment).
2. Commit `chore(ledger): TRUST refresh — <surface>` to `main`.
3. If the walk failed, ALSO file a new TBD-V## issue capturing the failure mode and link it from the row.

A walk on a stale build doesn't count — re-walk after every merge that touches the surface.
